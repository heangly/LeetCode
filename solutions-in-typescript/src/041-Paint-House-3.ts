function minCost4(houses: number[], cost: number[][], m: number, n: number, target: number): number {
  return 1
}

const houses = [0, 0, 0, 0, 0]
const cost = [
  [1, 10],
  [10, 1],
  [10, 1],
  [1, 10],
  [5, 1]
]
const m = 5
const n = 2
const target = 3

console.log(minCost4(houses, cost, m, n, target))
